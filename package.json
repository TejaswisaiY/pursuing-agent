{
  "name": "pursuing-agent",
  "version": "0.1.0",
  "type": "module",
  "description": "Personal accountability system — stateless polling agent on GitHub Actions",
  "main": "poll.js",
  "scripts": {
    "poll": "node poll.js",
    "seed": "node seed.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "dotenv": "^16.5.0"
  }
}
